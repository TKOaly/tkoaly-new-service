# This file contains environment variables used in Docker build process and the application itself.
# Postgres configuration (Postgres is not implemented yet)

# Postgres user
# POSTGRES_USER=postgres
# Postgres password
# POSTGRES_PASSWORD=postgres
# Postgres database
# POSTGRES_DB=postgres

# Debug (Django) MUST BE SET TO FALSE IF IN PRODUCTION!
DEBUG=False

# Secret key (Django) MUST BE CHANGED IF IN PRODUCTION!
SECRET_KEY=abcdefgh12345678

# Site name
SITE_NAME="TKO-äly ry"

# Site URL
SITE_URL="http://localhost:8000"

# Site port (only used with docker-compose)
PORT=5000